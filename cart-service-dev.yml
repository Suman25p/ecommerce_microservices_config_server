server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cart_db
    username: root
    password: SP25@hit

  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
